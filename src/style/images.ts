import { ImageSourcePropType } from "react-native";
import submittableLogoIcon from "../../assets/submittable-logo-icon.png";

export const IMAGES: { [key: string]: ImageSourcePropType } = {
  SUBMITTABLE_LOGO_ICON: submittableLogoIcon as ImageSourcePropType,
};
